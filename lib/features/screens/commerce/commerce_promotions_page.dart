import 'package:flutter/material.dart';

class CommercePromotionsPage extends StatelessWidget {
  const CommercePromotionsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Promociones y Cupones')),
      body: const Center(child: Text('Gestión de promociones y cupones (próximamente)')),
    );
  }
} 